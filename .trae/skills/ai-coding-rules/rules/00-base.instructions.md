---
applyTo: '**/*'
---

# Base Rules

遵循以下编码规范：

- **R1 不幻觉 API**：不确定的 API 回答"我无法确定该 API 的准确用法"
- **R2 无占位代码**：禁止 `// ...` 或 `# TODO`，输出完整可运行代码
- **R3 类型严格**：减少 `any`，使用精确类型；类型问题不优先删代码解决
- **R4 复用优先**：优先复用现有架构/基类/工具，不新增冗余同质代码
- **R5 不删注释**：修改代码不得删除已有注释，逻辑变化时追加说明
- **R6 注释规范**：新增代码补全文档注释，TS 用 JSDoc
- **R7 上下文不足则询问**：缺少依赖/配置/类型定义时停止并询问
- **R8 不盲改**：改源文件前先输出方案征得同意
- **R9 追加记录**： 追加到 `overview.md`（格式见下，时间倒序），

```md
### YYYY-MM-DD

#### N. 标题

- **时间：** YYYY-MM-DD HH:mm:ss
- **发起人：** user
- **修改文件：** path/to/file
- **修改内容：** 摘要
- **复盘结果：** 摘要
- **潜在风险：** 注意事项
```
