/**
 * 草稿持久化引擎（DraftEngine）
 *
 * @deprecated 请从 @lania-pro-components/utils 导入 DraftEngine
 * 此文件为向后兼容保留的 re-export 壳
 */
export {
  DraftEngine,
  createDraftEngine,
  localStorageStrategy,
  sessionStorageStrategy,
} from '@lania-pro-components/utils';
export type { DraftData, DraftStorage, DraftEngineConfig, DraftStatus } from '@lania-pro-components/utils';
