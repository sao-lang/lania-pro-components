/**
 * 表单性能监控组件（FormPerformanceMonitor）
 *
 * @deprecated 请从 @lania-pro-components/shared 导入 PerformanceMonitor
 * 此文件为向后兼容保留的 re-export 壳
 *
 * FormPerformanceMonitor 将在未来版本中移除，
 * 请使用组合方式接入 <PerformanceMonitor>。
 */
export { PerformanceMonitor as FormPerformanceMonitor } from '@lania-pro-components/shared';
export type { PerformanceMonitorProps as FormPerformanceMonitorProps } from '@lania-pro-components/shared';
export default null;
